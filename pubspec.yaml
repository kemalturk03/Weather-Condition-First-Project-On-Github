name: api_exercises
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  flutter_spinkit: "^4.1.2"
  geolocator: any
  http: any
  location: ^3.0.0
  provider: any
  sensors: ^0.3.2
  translator: any

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

  assets:
  - images/

  fonts:
    - family: SquadaOne
      fonts:
        - asset: fonts/SquadaOne-Regular.ttf